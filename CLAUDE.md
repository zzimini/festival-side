# CLAUDE.md — festival-side

> 개인 부업 프로젝트. 축제/행사 정보를 수집해 블로그 자동 발행(애드센스+쿠팡 파트너스)하고,
> 같은 데이터로 축제 달력 사이트와 릴스/쇼츠까지 확장하는 반자동 파이프라인.
> 이 파일은 프로젝트의 방향과 합의된 결정을 담은 컨텍스트다. 작업 전 반드시 읽을 것.

---

## 이 프로젝트가 뭔가

"이번 주말 무슨 축제 있지?"를 해결하는 행사 정보 서비스 + 그걸 수익화하는 콘텐츠 파이프라인.

- 행사 수집 → 준비물 도출(예: 흠뻑쇼→파란옷·방수팩, 야시장→돗자리·현금·지퍼백)
- 준비물을 쿠팡 파트너스 상품과 연동 + 애드센스
- 같은 행사 DB 위에 **축제 달력 사이트**를 얹고, **카드뉴스/릴스/쇼츠**로 홍보

**핵심 원칙: 행사 DB가 진실의 원천(source of truth).**
수집기·블로그·달력·릴스가 전부 이 DB 하나에서 읽어간다. 각 표현물이 따로 데이터를 갖지 않는다.

---

## 확정된 기술 결정 (뒤집지 말 것, 바꾸려면 사용자에게 먼저 확인)

- **언어: TypeScript로 통일.** 파이프라인·프론트 전부 TS. (파이썬 안 씀 — 언어 하나로 가는 이득이 스크래핑 라이브러리 이점보다 큼)
- **모노레포 1개.** 모듈별 레포 분리 금지. 전부 같은 DB 스키마를 공유하므로 한 레포에.
- **프론트/배포: Next.js + Vercel.** 행사 데이터는 자주 안 변하니 정적 생성(ISR)으로.
- **DB: Neon (서버리스 Postgres).** ORM은 Prisma. (MySQL이 편하면 PlanetScale도 대안)
- **크론: 처음엔 GitHub Actions 스케줄 워크플로 (무료).** 크롤링이 무거워지면 그때 VPS.
  - Vercel Cron은 함수 타임아웃이 짧아 무거운 크롤링엔 부적합.
- **발행 플랫폼: WordPress (REST API).** 티스토리·네이버는 자동 포스팅 API가 막혀서 제외.
- **패키지 매니저: pnpm.**

## 회사 자원과 완전 분리 (중요 — 겸업/소유권 이슈 방지)

이건 개인 부업이다. 회사(레드피치/G5) 자원을 절대 끌어다 쓰지 않는다.
- 서버·도메인·호스팅 전부 개인 명의
- 쿠팡 파트너스·애드센스·유튜브·인스타 계정 전부 개인 명의
- 회사 GPU 서버, 사내 API 키 사용 금지

---

## 가장 큰 리스크 2개 (설계가 이걸 피하는 방향이어야 함)

1. **구글 애드센스 "scaled content abuse"(대량 생성 콘텐츠 남용).**
   무검수 100% 자동 대량 발행은 심사 거절/사후 밴 대상.
   → **그래서 반자동. 발행 전 사람 검수 1스텝이 필수 보험.**
   → 달력 같은 실용 도구 + 내부 링크 구조가 저품질 리스크를 낮춤.
2. **어필리에이트/티켓사이트 ToS·크롤링.**
   쿠팡은 저품질 자동 생성 어필리에이트 사이트를 계속 걸러냄(승인·유지 모두 콘텐츠 질을 봄).
   상업 이벤트(흠뻑쇼·팝업·마켓)는 공공 API에 안 잡혀 크롤링해야 하는데 ToS·안티봇·구조 변경 이슈.

**운영 모델 = 반자동:**
- 자동: 수집 → 준비물 초안 → 쿠팡 매칭 → 렌더 → draft 큐에 적재
- 사람(하루 10~20분): draft 훑고 승인/수정 (어색한 글, 준비물 오탈, 상품 미스매치만 거름)
- 승인 후 자동: WordPress 발행 + (나중에) 유튜브/인스타 업로드

무검수 수십 개보다 검수 거친 하루 3~5개가 장기적으로 더 번다.

---

## 모듈 구성 (1~5가 코어, 6~9는 확장)

1. **collector (수집기)** — API 호출/크롤링으로 행사 목록 수집 → DB 저장.
   - 축제/공연류: TourAPI(한국관광공사), KOPIS(공연예술통합전산망), 공공데이터포털 = 정식 API, 쉬움.
   - 상업 이벤트: 크롤링 필요 = 제일 어려운 부분. 처음엔 수동 시드 리스트 + 반자동 크롤링.
2. **enricher (준비물 생성기)** — 행사를 LLM API에 던져 준비물 초안 생성. 프롬프트 다듬는 게 일.
3. **matcher (쿠팡 매칭기)** — 준비물 키워드로 쿠팡 파트너스 API 검색 → 딥링크 생성 → 저장. HMAC 서명 인증.
4. **composer (콘텐츠 조립기)** — 행사+준비물+쿠팡링크 → 블로그 글(HTML). 템플릿에 데이터 주입.
5. **publisher (발행기)** — 완성 글을 WordPress REST API로 **draft** 등록. (바로 publish 아님 — 검수 큐)
6. **검수 UI (선택)** — draft 훑고 승인. 없으면 WP 관리자에서 직접. (프론트 특기라 금방)
7. **카드뉴스/영상 렌더러 (나중)** — 이미지: HTML→이미지. 영상: Remotion(TS라 궁합 좋음)/FFmpeg.
8. **업로더 (나중)** — 유튜브 Data API / 인스타 Graph API(비즈니스 계정+FB 페이지 필수, 레이트리밋).
9. **스케줄러 (접착제)** — 위 모듈을 순서대로 도는 크론.

**달력 사이트**는 별도 모듈이 아니라 app/ 안의 Next.js 페이지. 행사 DB를 읽어 표현만 다르게 함.
전략적으로 달력이 미래의 본체 후보(재방문·북마크되는 도구 → 애드센스 수익 유리)지만, **데이터가 먼저다.**

---

## 목표 폴더 구조

```
festival-side/
├─ app/                 # Next.js — 달력 / 블로그 프론트 / 검수 UI
├─ lib/db.ts            # Prisma 클라이언트 (모두 여기서 DB 읽음)
├─ pipeline/            # 독립 실행 스크립트 (tsx로 실행)
│  ├─ collector/        # 1
│  ├─ enricher/         # 2
│  ├─ matcher/          # 3
│  ├─ composer/         # 4
│  └─ publisher/        # 5
├─ prisma/schema.prisma # ★ 진실의 원천 — DB 스키마
├─ .env.local           # 시크릿 (git 제외)
├─ .env.example         # 키 이름만 (값 없이, git 포함)
├─ .gitignore
└─ package.json
```

파이프라인은 `tsx pipeline/collector/index.ts`처럼 도는 독립 스크립트. Next.js 앱은 같은 DB를 읽기만.

---

## 지금 해야 할 세팅 순서 (이 순서 지킬 것)

1. `pnpm create next-app@latest .` (현재 폴더에), 그 후 `git init` → GitHub 레포 생성 → 첫 커밋 푸시.
2. `.gitignore`에 `.env.local` 확실히. `.env.example`엔 키 이름만. (시크릿 커밋 사고 방지)
3. **DB 먼저**: Neon 프로젝트 생성 → `DATABASE_URL`을 `.env.local`에.
4. `pnpm add -D prisma tsx && pnpm add @prisma/client` → `npx prisma init` → **schema.prisma 작성** → `npx prisma migrate dev`.
5. **빈 껍데기 상태로 Vercel 연결·배포해서 "푸시→자동배포" 초록불 먼저 확인.**
6. 그 다음 첫 모듈 코드.

**첫 진짜 파일은 `prisma/schema.prisma`.** 수집기·블로그·달력·릴스가 전부 공유하는 뼈대.
스키마에 처음부터 넣어둘 것(달력·릴스가 나중에 쓸 걸 아니까): 카테고리/타입, 지역, 좌표(위경도), 썸네일, 날짜(시작/종료), 출처(source), 원본 URL.

## 지금 하지 말 것

- 9개 폴더 미리 다 파기 (지금은 collector·publisher만)
- Turborepo/워크스페이스 등 모노레포 툴 (오버엔지니어링)
- 릴스·영상 라이브러리 (텍스트 파이프 검증 후)

---

## 진행 전략

`5(발행) → 2(준비물) → 4(조립)`으로 더미 데이터라도 글 하나를 실제 발행해 얇게 한 바퀴 →
그 다음 `1(수집)`·`3(쿠팡)` 붙이기 → 검증된 DB 위에 달력 → 마지막에 릴스/영상.

난이도: 쉬움 5 < 2 < 4 / 중간 3(인증만 넘으면) / 어려움 1(상업 이벤트 크롤링).

## 코드 아닌 준비물 (병렬 진행)

쿠팡 파트너스 가입·승인, 애드센스 심사, WordPress 호스팅, LLM API 키,
(나중) 유튜브/인스타 개발자 앱 등록.

## 주의

쿠팡 파트너스·인스타 릴스 API·애드센스 정책·티스토리 API는 자주 바뀐다.
각 API 착수 직전에 최신 문서/정책을 한 번 확인하고 시작할 것.
